# Copy this file to .env.local and fill in your values.

# URL of your chat backend. The /api/chat route will proxy requests here.
# If not set, the chat route returns a no-op response (UI still renders).
# Example: CHAT_API_URL=http://localhost:5000/api/chat
CHAT_API_URL=

# URL of the backend session creation endpoint.
# A POST to this URL (with the Authorization header) returns a session ID.
# Example: CHAT_SESSION_URL=http://localhost:5000/api/session
CHAT_SESSION_URL=

# Bearer token sent to the backend in the Authorization header.
# This is server-side only — it is never exposed to the browser.
# Example: CHAT_API_TOKEN=my-secret-token
CHAT_API_TOKEN=
